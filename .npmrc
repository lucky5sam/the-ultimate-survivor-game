; @vercel/analytics declares an optional peer of vue-router@^4, but this app is
; on vue-router@5. The mismatch is harmless (optional peer, stale range), so relax
; peer resolution here and, importantly, on Vercel's build so `npm install` there
; doesn't fail with ERESOLVE.
legacy-peer-deps=true
